// Scope selection will be reintroduced later for tenant users as a focused
// facility / operational-area context. The Super Admin keeps the organization
// navigation intentionally simple for now.
export const useOrganizationScope = () => ({})
